[build]
builder = "NIXPACKS"
buildCommand = "composer install --optimize-autoloader --no-dev --no-interaction && npm install && npm run build && php artisan view:clear && php artisan cache:clear && php artisan route:clear && php artisan config:clear"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT"

[php]
version = "8.2"
extensions = ["gd", "bcmath", "pdo_mysql", "zip", "mbstring", "xml", "curl"]

[env]
APP_ENV = "production"
APP_DEBUG = "false"
NODE_ENV = "production"
